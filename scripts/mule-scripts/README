Pouztie:Skript cez JMX zavola stop metodu na vsetky slave connectory (podla nazvu), nerobi dispose (destrcut). Tym padom je connector stale viditelny. Stop ma vyznam ze sa da po nom urobit start (cez JMX). Po dispose ho uz nie je mozne vytvorit bez restartu Mule.Postup pre nasadenie:  1. Instalvat tento skrupt a jeho denpendecie je mozne na lubovolny stroj, ktory ma pristup k JMX portu daneho Mule Slave ktory chceme ovladat (moze byt jedna instalacia pre ovladanie vsetkych Slave nodes)  2. zbuildovat TM projekt (pouzit Hudson job alebo lokalny maven build)  2. Vytvorit adresar pre skript (napr. c:\NDK\mule-scripts\)  3. Do vytvoreneho adresara nakopirovat:	- stopSlaveConnectors.bat (nachadza sa v projekte v \scripts\mule-scripts\scripts)	- adresar lib s potrebnymi dependencies (z \scripts\mule-scripts\ do c:\NDK\mule-scripts\lib)	- mule-scripts.jar (z \scripts\mule-scripts\ do c:\NDK\mule-scripts\)  4. spustit skript. Nastavime prikazovy riadok do adresara c:\NDK\mule-scripts\ a spustime skript kde <host> a <port> reprezentuju konkretne hodnoty:	stopSlaveConnectors.bat <host> <port>	(napr. stopSlaveConnectors.bat localhost 1097) - to na akom porte bezi JMX pre dany Slave je nastavene v slave.properties na danom Slave node